import React from "react";
import ReactDOM from "react-dom/client";
import { RecoverAccountProf } from "./components/AuthProfessional";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RecoverAccountProf />
  </React.StrictMode>
);
