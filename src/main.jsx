import React from "react";
import ReactDOM from "react-dom/client";
import { UpdateData } from "./components/AuthProfessional";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UpdateData />
  </React.StrictMode>
);
