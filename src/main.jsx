import React from "react";
import ReactDOM from "react-dom/client";
import { SeeAppointment } from "./Appointment";

import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SeeAppointment />
  </React.StrictMode>
);
